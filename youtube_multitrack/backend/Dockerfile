FROM python:3.12-slim

# System deps: ffmpeg (for yt-dlp post-processing), libsndfile (for soundfile)
RUN apt-get update && apt-get install -y --no-install-recommends \
    ffmpeg \
    libsndfile1 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Pre-download the Demucs model so first run is fast
RUN python -c "import demucs.pretrained; demucs.pretrained.get_model('htdemucs')" || true

COPY . .

ENV WORK_DIR=/tmp/multitrack_jobs
EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
